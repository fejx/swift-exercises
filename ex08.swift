// a function that receives two Int values as parameter and returns void
typealias ProgressCallback = (_ done: Int, _ total: Int) -> Void

// BONUS NOTE: function types must not have any argument labels, so the underscores are mandatory
